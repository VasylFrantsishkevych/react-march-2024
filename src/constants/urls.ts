const baseUrl = 'https://jsonplaceholder.typicode.com';

const urls = {
    usersUrls: {
        all: '/users',
    },
    postsUrls: {
      all: '/posts'
    },
    commentsUrls: {
      all: 'comments'
    }
}

export {urls, baseUrl}